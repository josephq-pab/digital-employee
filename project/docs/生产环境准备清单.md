# 生产环境准备清单

> 版本：v1.0（2026-04-16 21:00）
> 阶段：阶段6M（真实数据接入前准备）
> 用途：梳理生产环境配置准备项

---

## 一、生产环境 vs 当前环境

| 字段 | 当前环境 | 生产环境 |
|------|---------|---------|
| **运行环境** | 本地开发机（当前服务器） | 待确认 |
| **模型** | MiniMax-M2.5（当前配置） | 待确认（可能需要更换） |
| **数据** | 样本JSON文件 | 真实数据JSON文件 |
| **访问** | 内部访问 | 内部/外部访问（待确认） |

---

## 二、模型替换准备

### 2.1 当前模型状态

| 字段 | 内容 |
|------|------|
| **当前模型** | MiniMax-M2.5 |
| **调用方式** | 通过API（MiniMax MCP Server） |
| **配置文件** | openclaw gateway 配置 |
| **是否需更换** | 待确认（取决于业务方对模型的要求） |

### 2.2 模型替换准备项

| # | 准备项 | 当前状态 | 优先级 |
|---|--------|---------|--------|
| 1 | 确认是否需要更换模型 | ❌ 待确认 | P0 |
| 2 | 确认目标模型（如需更换） | ❌ 待确认 | P0 |
| 3 | 确认模型API配置（endpoint/key） | ❌ 待确认 | P1 |
| 4 | 在新模型上验证输出质量 | ❌ 待确认 | P1 |

### 2.3 模型更换判断

**建议判断标准：**
- 如果MiniMax-M2.5输出质量被业务方认可 → 不更换
- 如果业务方要求更换 → 执行模型替换
- 无论是否更换，当前阶段都是"准备"而非"执行"

---

## 三、环境变量准备

### 3.1 当前环境变量

| 变量名 | 当前值 | 说明 |
|-------|-------|------|
| **OPENCLAW_MODEL** | MiniMax-M2.5（默认） | 模型配置 |
| **SAMPLES_DIR** | /home/admin/.../samples/samples | 样本目录 |
| **OUTPUT_DIR** | project/data/output | 输出目录 |

### 3.2 生产环境需确认的变量

| # | 变量名 | 需确认内容 | 当前状态 | 优先级 |
|---|--------|---------|---------|--------|
| 1 | 模型API密钥 | 生产环境模型key | ❌ 待确认 | P0 |
| 2 | 模型endpoint | 生产环境API地址 | ❌ 待确认 | P0 |
| 3 | 数据目录路径 | 真实数据存放路径 | ❌ 待确认 | P1 |
| 4 | 输出目录路径 | 产出文件存放路径 | ❌ 待确认 | P1 |

---

## 四、路径/目录准备

### 4.1 当前目录结构

```
/home/admin/.openclaw/workspace-digital-employee/
├── project/
│   ├── src/
│   ├── scripts/
│   ├── data/
│   │   └── output/          ← 输出目录
│   └── docs/
├── samples/
│   └── samples/             ← 样本目录（当前数据）
│       ├── weekly_reports/
│       ├── project_ledger/
│       └── linked/
└── docs/
```

### 4.2 生产环境目录准备

| # | 目录 | 用途 | 当前状态 | 优先级 |
|---|------|------|---------|--------|
| 1 | samples/realtime/ | 存放真实周报/台账JSON | ✅ 已创建（2026-04-16） | P0 |
| 2 | project/data/output/ | 存放产出文件 | ✅ 已有 | — |
| 3 | project/logs/ | 存放运行日志 | ✅ 已创建（2026-04-16） | P2 |
| 4 | samples/samples_backup/ | 备份样本数据 | ❌ 待创建（手动备份即可） | P2 |

### 4.3 目录创建建议

```bash
# 真实数据目录（已创建）
mkdir -p /home/admin/.openclaw/workspace-digital-employee/samples/realtime
mkdir -p /home/admin/.openclaw/workspace-digital-employee/samples/realtime/weekly_reports
mkdir -p /home/admin/.openclaw/workspace-digital-employee/samples/realtime/project_ledger
mkdir -p /home/admin/.openclaw/workspace-digital-employee/samples/realtime/linked

# 日志目录（已创建）
mkdir -p /home/admin/.openclaw/workspace-digital-employee/project/logs

# 备份目录（手动备份）
cp -r samples/samples samples/samples_backup
```

### 4.4 真实数据目录使用说明

**方案A（直接替换）：**
```bash
# 替换周报文件
cp samples/realtime/weekly_reports/真实周报.json \
   samples/samples/weekly_reports/weekly_report_series_latest3.json

# 替换台账文件
cp samples/realtime/project_ledger/真实台账.json \
   samples/samples/project_ledger/project_ledger_latest3.json
```

**方案B（路径切换，推荐更清晰）：**
在run_prototype.py中增加路径配置，支持切换samples/samples 和 samples/realtime

---

## 五、依赖检查

### 5.1 当前Python依赖

| 依赖包 | 用途 | 当前状态 |
|-------|------|---------|
| Python 3.6.8+ | 运行环境 | ✅ 已满足 |
| json | 数据解析 | ✅ 内置 |
| os | 路径操作 | ✅ 内置 |

### 5.2 生产环境依赖检查清单

| # | 检查项 | 命令 | 当前状态 |
|---|--------|------|---------|
| 1 | Python版本 | `python3 --version` | ✅ 3.6.8 |
| 2 | 脚本可执行 | `python3 scripts/run_prototype.py` | ✅ 7/7 PASS |
| 3 | 输出目录可写 | `touch project/data/output/test` | ✅ 可写 |
| 4 | 样本目录可读 | `ls samples/samples/` | ✅ 可读 |

---

## 六、启动方式确认

### 6.1 当前启动方式

```bash
# 手动运行
cd project
python3 scripts/run_prototype.py

# 输出文件到 project/data/output/
```

### 6.2 生产环境启动方式选项

| # | 启动方式 | 说明 | 适用场景 |
|---|---------|------|---------|
| 1 | 手动CLI | python3 scripts/run_prototype.py | 初期验证 |
| 2 | 定时cron | 定期自动运行 | 周期性报告 |
| 3 | Webhook触发 | 外部系统触发 | 有API需求时 |
| 4 | OpenClaw agent调用 | 通过agent触发 | 与其他系统集成 |

### 6.3 当前阶段建议

**当前阶段建议：保持手动CLI方式**

理由：
- 当前阶段是"准备阶段"，不需要自动化
- 手动运行可观察每一步输出，便于验证
- 等真实数据接入稳定后再考虑自动化

---

## 七、日志与回滚准备

### 7.1 当前日志状态

| 字段 | 内容 |
|------|------|
| **当前日志** | 无专门日志文件（仅控制台输出） |
| **输出文件** | project/data/output/ 目录 |
| **备份** | 无自动备份 |

### 7.2 日志与回滚准备项

| # | 准备项 | 当前状态 | 优先级 |
|---|--------|---------|--------|
| 1 | 建立运行日志记录 | ❌ 待建立 | P2 |
| 2 | 建立输出文件版本记录 | ❌ 待建立 | P2 |
| 3 | 建立样本数据备份 | ❌ 待建立 | P2 |

---

## 八、当前已具备 vs 待确认

### 8.1 已具备（可直接用）

| # | 项 | 说明 |
|---|------|------|
| 1 | Python运行环境 | Python 3.6.8 ✅ |
| 2 | 原型可运行 | 7/7 PASS ✅ |
| 3 | 输出目录 | project/data/output/ ✅ |
| 4 | 冒烟测试 | 7/7 PASS ✅ |
| 5 | 样本数据 | samples/samples/ ✅ |

### 8.2 待确认（需在6M阶段确认）

| # | 项 | 优先级 |
|---|------|--------|
| 1 | 是否更换模型 | P0 |
| 2 | 真实数据JSON格式 | P0 |
| 3 | 真实数据文件存放路径 | P1 |
| 4 | 日志目录 | P2 |
| 5 | 启动方式 | P2 |

---

## 九、下一步动作

| # | 动作 | 负责方 | 优先级 |
|---|------|--------|--------|
| 1 | 确认是否需要更换模型 | 业务方/技术方 | P0 |
| 2 | 确认真实数据存放路径 | 项目方 | P1 |
| 3 | 建立真实数据目录 | 开发方 | P1 |
| 4 | 创建日志目录 | 开发方 | P2 |

---

*本清单为生产环境准备项梳理，待在阶段6M中逐步确认和准备。*
