# 模型使用策略确认记录

> 版本：v3.0（发出版本，2026-04-16 21:31）
> 阶段：阶段6M（项目方回填等待与回填后判定轮）
> 用途：**请业务方/项目方选择一个方案并签字确认**

---

## 一、当前模型状态

| 字段 | 内容 |
|------|------|
| 当前使用模型 | MiniMax-M2.5 |
| 调用方式 | OpenClaw MCP Server |
| B3业务反馈 | 模拟数据场景下准确率可接受（条件通过） |

---

## ★ 当前推荐：方案C（先不定，等真实数据验证后决定）

**开发方推荐选择方案C，理由：**
1. B3在模拟数据上已条件通过，先保持现状
2. 真实数据接入后做第一轮验证，再判断是否需要切换
3. 切换模型有成本和风险，等有充分理由时再切
4. 不影响当前任何接入准备工作

**若项目方无异议，将默认按方案C执行。** |

---

## 二、三个可选方案

### 方案A：继续使用 MiniMax-M2.5（推荐）

| 字段 | 内容 |
|------|------|
| **模型** | MiniMax-M2.5（当前配置） |
| **优点** | 无需切换成本；B3已条件通过；风险最低 |
| **风险** | 生产环境若需特定模型（如合规要求）则不适用 |
| **适用时机** | 当前推荐 |
| **是否需要开发** | 否 |
| **模型切换时间** | 不需要切换 |

### 方案B：切换到指定模型

| 字段 | 内容 |
|------|------|
| **模型** | 由业务方指定（如GPT-4/Claude/其他） |
| **优点** | 满足特定业务/合规要求 |
| **风险** | 需要API配置；输出质量需重新验证；切换有成本 |
| **适用时机** | 业务方有明确指定的模型要求 |
| **是否需要开发** | 是（OpenClaw配置变更） |
| **模型切换时间** | 确认后约1-2天 |

**若选择方案B，请填写：**
| 填写项 | 项目方填写 |
|-------|---------|
| 指定模型名称 | ______________ |
| API endpoint | ______________ |
| API key | ______________（保密） |
| 切换时间要求 | ______________ |

### 方案C：先不定（推荐保守策略）

| 字段 | 内容 |
|------|------|
| **模型** | MiniMax-M2.5（暂时不变） |
| **优点** | 保持现状；等真实数据接入并验证B3后再决定 |
| **风险** | 若后续需切换，延迟决定 |
| **适用时机** | 当前无法判断，希望先验证真实数据效果 |
| **是否需要开发** | 否 |
| **模型切换时间** | 真实数据验证B3通过后决定 |

---

## 三、推荐决策

**基于当前信息（B3条件通过），推荐：方案C（先不定）**

理由：
1. B3在模拟数据上已条件通过，先保持现状
2. 真实数据接入后做第一轮验证，再判断是否需要切换
3. 切换模型有成本和风险，等有充分理由时再切

---

## 四、【请业务方/项目方选择】

**★ 项目方只需回复以下两项：**

```
1. 选择哪个方案？（在□打勾）
   □ 方案A：继续使用 MiniMax-M2.5
   □ 方案B：切换到指定模型（需补充填写下方信息）
   □ 方案C：先不定，等真实数据验证后决定

2. 若选方案B，请补充：
   指定模型：________________
   API endpoint：________________
   API key：________________（保密）
```

---

## 五、模型切换的前置条件（若选择方案B）

**切换前必须满足：**

| # | 前置条件 | 状态 |
|---|---------|------|
| 1 | API endpoint 已确认 | ☐ |
| 2 | API key 已获取 | ☐ |
| 3 | OpenClaw配置已变更 | ☐ |
| 4 | 冒烟测试通过（7/7 PASS） | ☐ |
| 5 | 业务方验收通过 | ☐ |

---

## 六、当前状态

| 字段 | 内容 |
|------|------|
| **当前模型** | MiniMax-M2.5 |
| **推荐方案** | 方案C（先不定） |
| **本记录状态** | ⏳ **待业务方/项目方选择并签字** |

---

*请业务方/项目方选择一个方案并填写签字栏，本模板方可变为已确认状态。*
