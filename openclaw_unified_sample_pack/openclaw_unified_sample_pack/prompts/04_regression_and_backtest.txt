你现在基于统一样本包执行回归与稳态验证。

推荐顺序：
1. 先用 `slices/01_dev_core_december.json` 跑基线
2. 再用 `slices/02_holdout_validation_november.json` 做未见样本验证
3. 再用 `slices/03_stress_regression_aug_sep.json` 做压力回归

输出《回归验证报告》：
一、基线切片结果
二、留出验证结果
三、压力回归结果
四、问题差异
五、是否建议进入下一阶段
